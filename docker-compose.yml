version: '3.8'
services:
  astraforge:
    build: .
    container_name: astraforge-ide
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    volumes:
      - ./astra_data:/app/data
