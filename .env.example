# AstraForge IDE — Environment Configuration
# Copy: cp .env.example .env
# Then fill in the API keys for your preferred providers.
# You only need ONE provider to get started.

# =============================================================================
# LLM PROVIDERS — At least one required
# =============================================================================

# OpenAI
OPENAI_API_KEY=sk-your-openai-api-key

# Anthropic (Claude)
ANTHROPIC_API_KEY=sk-ant-your-anthropic-api-key

# xAI (Grok)
GROK_API_KEY=xai-your-grok-api-key

# OpenRouter (access 100+ models with one key — recommended for beginners)
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-api-key

# Ollama (local, free — set endpoint + model name)
OLLAMA_ENDPOINT_API_KEY=http://127.0.0.1:11434
OLLAMA_MODEL_API_KEY=llama3

# LM-Studio (local OpenAI-compatible)
LMSTUDIO_API_KEY=http://localhost:1234/v1

# =============================================================================
# PER-AGENT OVERRIDES (optional — defaults to global keys above)
# Format: AGENT_{N}_PROVIDER, AGENT_{N}_MODEL, AGENT_{N}_KEY
# N is 1–5 (Nexus, Vanguard, Prism, Helix, Cipher)
# =============================================================================
# AGENT_1_PROVIDER=OpenRouter
# AGENT_1_MODEL=openai/gpt-4o
# AGENT_1_KEY=sk-or-v1-...

# =============================================================================
# SERVER / SECURITY (production only)
# =============================================================================
# ASTRA_SERVER_TOKEN=your-random-secret-token  # Required in production
# PORT=3000
# NODE_ENV=production

# =============================================================================
# OPTIONAL INTEGRATIONS
# =============================================================================
# GITHUB_TOKEN=ghp_your-github-token
