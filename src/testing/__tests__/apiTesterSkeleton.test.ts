import { APITester } from '../apiTester';

describe('APITester Skeleton', () => {
  it('should be instantiable', () => {
    const tester = new APITester();
    expect(tester).toBeDefined();
  });
});
