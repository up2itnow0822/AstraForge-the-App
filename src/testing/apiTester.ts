import { spawn, SpawnOptions } from 'child_process';
import { EventEmitter } from 'events';

export interface TestResult {
}

export interface APITestConfig {
}

export class APITester extends EventEmitter {




















}
