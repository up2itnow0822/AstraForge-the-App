import { app, BrowserWindow, ipcMain } from 'electron';
import * as path from 'path';
import * as express from 'express';
import { LocalOrchestrationEngine } from './$1';

class AstraForgeApp {








}

// Initialize app
const astraForgeApp = new AstraForgeApp();

app.on('ready', () => {
});

app.on('window-all-closed', () => {
});

app.on('activate', () => {
});
